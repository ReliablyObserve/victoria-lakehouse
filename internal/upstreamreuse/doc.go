// Package upstreamreuse contains only tests. See upstreamreuse_test.go
// for the rationale — this package exists to give the tests a home
// that the Go test runner discovers automatically (every other
// package under internal/ has production code, so colocating these
// guards there would mix concerns).
package upstreamreuse
