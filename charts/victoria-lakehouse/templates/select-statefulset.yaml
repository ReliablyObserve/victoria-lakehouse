{{- if .Values.select.enabled }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "victoria-lakehouse.fullname" . }}-select
  labels:
    {{- include "victoria-lakehouse.componentLabels" (dict "root" . "component" "select") | nindent 4 }}
spec:
  replicas: {{ .Values.select.replicaCount }}
  serviceName: {{ include "victoria-lakehouse.fullname" . }}-select-headless
  selector:
    matchLabels:
      {{- include "victoria-lakehouse.componentSelectorLabels" (dict "root" . "component" "select") | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        {{- with .Values.select.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "victoria-lakehouse.componentSelectorLabels" (dict "root" . "component" "select") | nindent 8 }}
        {{- with .Values.select.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ default (printf "%s-select" (include "victoria-lakehouse.fullname" .)) .Values.select.serviceAccount.name }}
      terminationGracePeriodSeconds: {{ .Values.select.terminationGracePeriodSeconds }}
      {{- with (include "victoria-lakehouse.podSecurityContext" (dict "component" .Values.select "common" .Values.common)) }}
      securityContext:
        {{- . | nindent 8 }}
      {{- end }}
      {{- with .Values.select.initContainers }}
      initContainers:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: select
          image: {{ include "victoria-lakehouse.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - "--lakehouse.config=/etc/lakehouse/config.yaml"
            - "--lakehouse.role=select"
            {{- range $key, $val := .Values.select.extraArgs }}
            - "--{{ $key }}={{ $val }}"
            {{- end }}
          ports:
            - name: http
              containerPort: {{ include "victoria-lakehouse.servicePort" . }}
              protocol: TCP
          {{- with (include "victoria-lakehouse.securityContext" (dict "component" .Values.select "common" .Values.common)) }}
          securityContext:
            {{- . | nindent 12 }}
          {{- end }}
          {{- with (include "victoria-lakehouse.resources" (dict "component" .Values.select "common" .Values.common)) }}
          resources:
            {{- . | nindent 12 }}
          {{- end }}
          {{- with .Values.select.extraEnv }}
          env:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.select.extraEnvFrom }}
          envFrom:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            - name: config
              mountPath: /etc/lakehouse
              readOnly: true
            - name: data
              mountPath: /data/lakehouse
            {{- with .Values.select.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: {{ .Values.select.probe.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.select.probe.liveness.periodSeconds }}
            failureThreshold: {{ .Values.select.probe.liveness.failureThreshold }}
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: {{ .Values.select.probe.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.select.probe.readiness.periodSeconds }}
            failureThreshold: {{ .Values.select.probe.readiness.failureThreshold }}
          startupProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: {{ .Values.select.probe.startup.periodSeconds }}
            failureThreshold: {{ .Values.select.probe.startup.failureThreshold }}
        {{- with .Values.select.extraContainers }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      volumes:
        - name: config
          configMap:
            name: {{ include "victoria-lakehouse.fullname" . }}-config
        {{- with .Values.select.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with (include "victoria-lakehouse.nodeSelector" (dict "component" .Values.select "common" .Values.common)) }}
      nodeSelector:
        {{- . | nindent 8 }}
      {{- end }}
      {{- with (include "victoria-lakehouse.tolerations" (dict "component" .Values.select "common" .Values.common)) }}
      tolerations:
        {{- . | nindent 8 }}
      {{- end }}
      {{- with (include "victoria-lakehouse.affinity" (dict "component" .Values.select "common" .Values.common)) }}
      affinity:
        {{- . | nindent 8 }}
      {{- end }}
      {{- with .Values.select.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.select.priorityClassName }}
      priorityClassName: {{ . }}
      {{- end }}
  {{- if .Values.select.persistence.enabled }}
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes:
          {{- toYaml .Values.select.persistence.accessModes | nindent 10 }}
        {{- with .Values.select.persistence.storageClass }}
        storageClassName: {{ . }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.select.persistence.size }}
  {{- end }}
{{- end }}
