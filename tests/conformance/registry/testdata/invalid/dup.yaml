- id: vl.select.query.wildcard
  title: "/select/logsql/query wildcard"
  surface: vl
  kind: select
  origin: native
  expect: pass
  targets: [hot, cold, global]
  seed: [logs.base]
  upstream: { route: /select/logsql/query }
  request: { method: GET, path: /select/logsql/query, params: { query: "*", limit: "100" } }
  compare: { type: ndjson-multiset, project: [_time, _msg, _stream] }
  layers: [api]
  pending: true
- id: vl.select.query.wildcard
  title: "/select/logsql/query wildcard"
  surface: vl
  kind: select
  origin: native
  expect: pass
  targets: [hot, cold, global]
  seed: [logs.base]
  upstream: { route: /select/logsql/query }
  request: { method: GET, path: /select/logsql/query, params: { query: "*", limit: "100" } }
  compare: { type: ndjson-multiset, project: [_time, _msg, _stream] }
  layers: [api]
  pending: true
